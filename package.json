{
  "name": "@jdrly/teamwork-agent-tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "teamwork-agent": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm build && node --test tests/*.test.js",
    "prepare:local": "pnpm build && node scripts/install-local.mjs"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=20"
  },
  "files": [
    ".codex-plugin",
    "dist",
    "skills",
    "README.md",
    "LICENSE"
  ]
}
